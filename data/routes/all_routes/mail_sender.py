import smtplib

def send_mail(user, mail_to, subject, text):  # Отправка сообщений на электронную почту
    pass
    # HOST = "www.uc.osu.ru"
    # FROM = "just_fun@gmail.com"
    # BODY = "\r\n".join((
    #     "From: %s" % FROM,
    #     "To: %s" % mail_to,
    #     "Subject: %s" % subject,
    #     "",
    #     f'{user}, {text}'
    # ))
    # server = smtplib.SMTP(HOST)
    # server.sendmail(FROM, [mail_to], BODY)
    # server.quit()